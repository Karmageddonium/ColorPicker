<UserControl x:Class="ColorPickerWPF.Core.ColorPickRect"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ColorPickerWPF.Core"
             mc:Ignorable="d" x:Name="colorPickRect"
             d:DesignHeight="50" d:DesignWidth="50" DataContext="{Binding RelativeSource={RelativeSource Self}}">
    <Grid>
        <Border BorderBrush="Black" BorderThickness="1" MouseLeftButtonDown="Border_MouseLeftButtonDown">
            <Grid x:Name="ColorDisplayGrid" Background="{Binding Path=Color, ElementName=colorPickRect}"/>
        </Border>
    </Grid>
</UserControl>
